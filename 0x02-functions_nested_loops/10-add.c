#include"main.h"
/**
 * add - adds two integers and return result
 * @a: first parameter
 * @b: second parameter
 * Return: return the result of a + b
*/

int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
